---
title: 大纲
author: DBAAZzz
date: 2026/01/03
categories:
  - 面试
  - 网络
tags:
  - HTTP
  - HTTPS
  - TCP/IP
---

## 一、Node.js 基础

### 1.1 核心概念

- Node.js 的定义与特点
- 事件驱动与非阻塞 I/O
- 单线程模型
- V8 引擎
- CommonJS 模块系统
- ES Modules 支持

### 1.2 全局对象与内置模块

- global 对象
- process 对象
- Buffer 类
- **dirname** 和 **filename**
- 常用内置模块(fs, path, http, stream 等)

## 二、事件循环机制

### 2.1 事件循环原理

- 事件循环的六个阶段
- timers、pending callbacks、idle/prepare、poll、check、close callbacks
- 微任务与宏任务
- process.nextTick() 的特殊性
- setImmediate() vs setTimeout()

### 2.2 异步编程

- 回调函数(Callback)
- Promise
- async/await
- EventEmitter
- 错误处理机制

## 三、模块系统

### 3.1 CommonJS

- require() 加载机制
- module.exports 与 exports
- 模块缓存机制
- 循环引用问题

### 3.2 ES Modules

- import/export 语法
- .mjs 文件
- package.json 的 "type" 字段
- CommonJS 与 ES Modules 的互操作

### 3.3 包管理

- npm/yarn/pnpm
- package.json 配置
- 依赖版本管理
- 包的发布与管理

## 四、核心模块深入

### 4.1 文件系统(fs)

- 同步与异步操作
- Stream 流式读写
- 文件监听(fs.watch/fs.watchFile)
- 文件描述符

### 4.2 Stream 流

- 流的四种类型(Readable, Writable, Duplex, Transform)
- pipe() 方法
- 背压(Backpressure)机制
- 流的事件(data, end, error, finish)

### 4.3 Buffer

- Buffer 的创建与使用
- 字符编码
- Buffer 与字符串互转
- Buffer 的性能优化

### 4.4 网络模块

- http/https 模块
- net 模块(TCP)
- dgram 模块(UDP)
- 创建 HTTP 服务器
- 请求与响应处理

## 五、进程与线程

### 5.1 进程管理

- child_process 模块
- spawn、exec、execFile、fork 的区别
- 进程间通信(IPC)
- 父子进程通信

### 5.2 多进程架构

- cluster 模块
- 负载均衡
- 多核 CPU 利用
- PM2 进程管理

### 5.3 Worker Threads

- 工作线程的创建
- 线程间通信
- SharedArrayBuffer
- 适用场景

## 六、性能优化

### 6.1 内存管理

- V8 内存结构
- 新生代与老生代
- 垃圾回收机制(Scavenge, Mark-Sweep, Mark-Compact)
- 内存泄漏排查
- heapdump 与内存快照

### 6.2 性能监控

- process.memoryUsage()
- process.cpuUsage()
- 性能钩子(perf_hooks)
- 性能分析工具(clinic.js, 0x)

### 6.3 优化策略

- 缓存策略
- 连接池
- 代码优化(避免同步操作、合理使用 Stream)
- 压缩与静态资源优化

## 七、安全性

### 7.1 常见安全问题

- SQL 注入
- XSS 攻击
- CSRF 攻击
- 命令注入
- 路径遍历

### 7.2 安全实践

- 输入验证与过滤
- 使用 helmet 等安全中间件
- HTTPS 配置
- 环境变量管理
- 依赖安全审计(npm audit)

## 八、调试与测试

### 8.1 调试技巧

- console 调试
- Node.js 内置调试器
- Chrome DevTools 调试
- VSCode 调试配置
- 日志管理

### 8.2 测试

- 单元测试(Mocha, Jest)
- 断言库(Chai, Assert)
- 测试覆盖率(nyc, istanbul)
- 集成测试
- Mock 与 Stub

## 九、框架与生态

### 9.1 Web 框架

- Express.js
- Koa.js
- Nest.js
- Fastify
- 中间件机制

### 9.2 数据库操作

- MySQL 驱动(mysql2)
- MongoDB 驱动(mongoose)
- Redis 驱动(ioredis)
- ORM 工具(Sequelize, TypeORM)
- 连接池管理

### 9.3 实时通信

- WebSocket
- Socket.io
- Server-Sent Events(SSE)
- 长轮询

## 十、部署与运维

### 10.1 部署方式

- Docker 容器化
- PM2 进程管理
- 负载均衡(Nginx)
- 反向代理
- 灰度发布

### 10.2 日志与监控

- 日志收集(Winston, Bunyan)
- 错误追踪
- 性能监控(APM)
- 告警机制

### 10.3 运维实践

- 环境变量配置
- 优雅重启与关闭
- 健康检查
- 容错与降级

## 十一、高级主题

### 11.1 微服务架构

- 微服务设计原则
- 服务间通信(REST, gRPC)
- 服务发现与注册
- 分布式追踪

### 11.2 消息队列

- RabbitMQ
- Kafka
- Redis 消息队列
- 消息可靠性保证

### 11.3 TypeScript 与 Node.js

- TypeScript 配置
- 类型定义(@types)
- 编译与运行
- 常见问题与解决方案

## 十二、实战场景

### 12.1 常见场景实现

- 文件上传下载
- 数据导入导出
- 定时任务
- 爬虫实现
- SSR 服务端渲染

### 12.2 性能优化案例

- 高并发场景优化
- 大文件处理
- 数据库查询优化
- 缓存策略应用

### 12.3 问题排查

- CPU 占用过高
- 内存泄漏排查
- 请求响应慢
- 死锁问题
